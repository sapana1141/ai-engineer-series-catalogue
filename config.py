# Path to the unified SQLite database (Stores metadata, FTS5 index, and embeddings)
DB_PATH = "catalogue.db"

# The embedding model used for semantic vector search
EMBED_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
